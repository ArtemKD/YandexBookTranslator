<UserControl x:Class="YandexBookTranslator.Views.Controls.TranslateControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YandexBookTranslator.Views.Controls"
             xmlns:ViewModels="clr-namespace:YandexBookTranslator.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <ViewModels:TranslateViewModel/>
    </UserControl.DataContext>
    <Grid>
        <StackPanel>
            <Label Content="{Binding Title}"/>
            <StackPanel Orientation="Horizontal">
                <Label Content="Директория перевода"/>
                <Label Content="{Binding SavePath}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Файлы для перевода"/>
                <Label Content="{Binding FileList}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
